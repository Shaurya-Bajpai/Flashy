<?xml version="1.0" encoding="utf-8"?>
<!--
    Backup rules for API < 31.
    DataStore writes to files/datastore/ — using domain="file" excludes it
    from Auto Backup so stale preferences from another device are never restored.
-->
<full-backup-content>
    <exclude domain="file" path="datastore" />
</full-backup-content>
